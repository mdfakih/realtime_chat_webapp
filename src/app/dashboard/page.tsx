import Button from '@/components/ui/Button';
import { FC } from 'react';

interface pageProps {}

const page: FC<pageProps> = ({}) => {
  return <Button size="sm">hello</Button>;
};

export default page;
